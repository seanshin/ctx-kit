/**
 * Ruler adapter placeholder.
 *
 * `ctxkit sync` is implemented internally (src/core/sync.ts) with zero
 * dependencies, covering the common targets (Claude, Cursor, Gemini,
 * Copilot, Windsurf). If 30+ targets or Ruler-specific transforms are
 * ever needed, wire Ruler here behind the same syncRules() interface —
 * after verifying its LICENSE.
 */
export {};
